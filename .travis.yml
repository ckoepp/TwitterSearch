language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
install:
  - "pip install -r requirements.txt --use-mirrors"
  - "pip install httpretty=0.6.2 --use-mirrors"
script: 
  - nosetests -v
branches:
  only:
    - master
    - devel
